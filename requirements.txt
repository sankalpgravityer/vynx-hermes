fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
PyYAML==6.0.2
python-dotenv==1.0.1
google-genai>=2.3.0
pytest==8.3.4

# Validates the reprice script SQL against the real Postgres grammar
# (libpg_query), so a syntax error surfaces in tests rather than on a database.
pglast==8.4

# scripts/reprice_review_db.py talks to Postgres directly.
psycopg[binary]==3.3.4
openpyxl==3.1.5

# The imagery background check reads pixels — app/imaging/background.py samples a
# border ring to tell a genuine cut-out from a photograph whose row merely claims
# one. Header-only decode plus one masked statistic, so it stays cheap.
Pillow==12.3.0

# /v1/readability. PP-OCRv6 detection + recognition as ONNX, which is what lets
# this run on CPU inside the endpoint's ~1.5s budget with no model call and no
# PaddlePaddle build. `rapidocr`, not the retired `rapidocr_onnxruntime`; since
# 2.0.6 it does not pull a runtime of its own, hence the explicit onnxruntime.
rapidocr==3.9.2

# PINNED, and not to the latest on purpose.
#
# onnxruntime 1.29.0 fails to load on Windows 10 with only the VC++ 2015-2019
# redistributable (14.29) installed: `WinError 1114, A dynamic link library (DLL)
# initialization routine failed`, raised by onnxruntime.dll itself before Python
# is involved. Newer builds want the 2015-2022 redistributable (14.40+). 1.20.1
# is the newest release that loads against 14.29 and is the first with cp313
# wheels, so it is the one version that covers both.
#
# Linux is unaffected — any 1.2x works there. Raise this pin once every machine
# that runs Hermes has the 2022 redistributable, and note that accuracy is
# unchanged either way: same ONNX models, same operators.
onnxruntime==1.20.1

# numpy is a rapidocr dependency, named here because app/imaging/legibility.py
# imports it directly for the Laplacian and the frame statistics.
numpy>=2.0

# FastAPI needs this to parse multipart/form-data, which is how /v1/readability
# takes the image. Base64 in JSON would add a third to the payload, and upload is
# the largest item in that endpoint's latency budget.
python-multipart==0.0.32
